# Private keys are machine-specific and thus ignored
# todo: rename keys host-specifically and put public keys in the repo
/id_*

# known_hosts and public_hosts are public, but all the others are not.
/*_hosts
!/known_hosts
!/public_hosts
